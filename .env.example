# Copy to .env for local overrides. NEVER put secrets in the frontend.

# Narrative provider switch. Template-only this period; when 'true' the LLM
# provider is constructed but still degrades to the template (the real LLM call
# is a future edge function). See src/narrative/index.ts.
VITE_USE_LLM=false
