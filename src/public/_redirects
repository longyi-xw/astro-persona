# Root has no page under the i18n `prefix` strategy → send to the default locale.
# All other routes are prerendered; unmatched paths fall to the 404 page
# (wrangler `not_found_handling`). Honored by Cloudflare static assets, Pages & Netlify.
/    /zh/    302
